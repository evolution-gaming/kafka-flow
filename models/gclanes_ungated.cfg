\* spec: GroupCommitLanes
\* expect: VIOLATES INV_OffsetWithinDurable
\* G2 safety control (marker-lane race): the flush-blocks-then-schedule coupling
\* dropped. The flow schedules an offset before its writes are durable and the
\* marker lane commits it -- the committed offset leads the durable prefix.
CONSTANTS N = 3  Cap = 2
  Gated = FALSE  SharedBudget = FALSE  MarkerSelfCommit = TRUE
  EnableAbort = FALSE  AtomicDurable = TRUE
SPECIFICATION SafeSpec
INVARIANTS TypeOK INV_OffsetWithinDurable
