\* spec: RecoveryRead
\* expect: VIOLATES-REFINEMENT RefAtomic
\* flags: -deadlock
\* THEOREM RecoveryRead => RecoveryReadAtomic is FALSE for the design as first merged
\* (finding F-10, issue #850): unique per-assignment transactional.ids (nobody aborts
\* crashed A's open transaction) + the read bounded at its own read_committed endOffsets --
\* under the pinned platform fact that is the LSO, pinned below B's committed S3. The
\* Capture step picks a bound that already excludes S3, so NO instant's committed set
\* equals what the read returns: the step simulation fails at Capture itself.
\* Replicated live: an 85 ms "successful" read missing a committed snapshot.
\* Fact-sweep sibling: recoveryread_endoffsets_hw -- the SAME design point under the other
\* reading of ext(K2) (EndOffsetsIsLSO = FALSE) HOLDS. The verdict flips on one platform
\* fact, which is what made ext(K2) load-bearing and this defect invisible under the
\* merged-in reading. This failing config is also the mapping's non-vacuity evidence.
SPECIFICATION Spec
CONSTANTS StableId = FALSE  HwTarget = FALSE  Foreign = FALSE  EndOffsetsIsLSO = TRUE  Truncation = FALSE  Tripwire = FALSE  FreezeObserved = TRUE
INVARIANT TypeOK
PROPERTY RefAtomic
