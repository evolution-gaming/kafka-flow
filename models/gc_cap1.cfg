\* spec: GroupCommit
\* expect: HOLDS
\* No batching: every write is its own transaction (the maximal split of a flush).
\* Termination and the offset-within-durable safety both hold.
CONSTANTS N = 3  Cap = 1  Gated = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK INV_OffsetWithinDurable
PROPERTIES Termination
