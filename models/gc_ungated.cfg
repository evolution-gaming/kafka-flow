\* spec: GroupCommit
\* expect: VIOLATES INV_OffsetWithinDurable
\* Safety control: the coupling dropped -- schedule an offset before its writes
\* are durable, so the committed offset leads the durable prefix (recovery loses writes).
CONSTANTS N = 3  Cap = 2  Gated = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK INV_OffsetWithinDurable
