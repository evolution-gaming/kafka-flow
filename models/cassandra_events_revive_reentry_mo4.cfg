\* spec: Cassandra
\* expect: VIOLATES INV_NoCorruptDurable
\* Higher-bound (MaxOffset=4) negative paired with cassandra_events_refines_mo4. With the F-6 fold-result
\* comparison (FloorGuard=TRUE) but WITHOUT the structural floor filter (FloorFilter=FALSE), the unfenced
\* journal still corrupts the durable cell. At MaxOffset=4 the genuine delete-residue REVIVE (pre-delete rows
\* surviving past a tombstone, re-folded durably) is reachable, whereas at MaxOffset=3 only its
\* under-representation LOSS dual is (finding B-1). TLC may report the shallower loss trace first; either way
\* INV_NoCorruptDurable is violated. With the paired positive HOLDING, the FloorFilter fix is load-bearing at
\* the bound where the flagship revive appears.
SPECIFICATION Spec
CONSTANTS MaxOffset = 4  Guarded = TRUE  Tombstone = TRUE  Fix = TRUE  Spurious = FALSE  TombFloor = TRUE  MonotonicInit = TRUE  ReapTTL = FALSE  EventsRecovery = TRUE  JournalOrder = TRUE  FloorGuard = TRUE  FloorFilter = FALSE  SkipTomb = FALSE
INVARIANT INV_NoCorruptDurable
