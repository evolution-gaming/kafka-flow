\* spec: ReplayFence
\* expect: VIOLATES INV_NoSelfFence
\* The bug, SAFETY framing (finite counterexample, no fairness needed): the
\* legitimate owner presents its lagging offset and is CAS-rejected.
CONSTANTS MaxOffset = 3  Fix = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK INV_NoSelfFence
