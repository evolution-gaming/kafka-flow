\* spec: ReplayFence
\* expect: HOLDS
\* The fix: presents the recovered high-water -> never CAS-rejected (safety holds)
\* and completes under weak fairness (liveness holds).
CONSTANTS MaxOffset = 3  Fix = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK INV_NoSelfFence
PROPERTIES OwnerCompletes
