\* spec: GroupCommit
\* expect: HOLDS
\* One holder drains the whole queue (Cap = N).
CONSTANTS N = 3  Cap = 3  Gated = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK INV_OffsetWithinDurable
PROPERTIES Termination
