\* spec: Kafka
\* expect: VIOLATES INV_CaptureCoupled
\* flags: -deadlock
\* The same cause at the coupling invariant: decoupling lets an alive zombie hold the current
\* captured generation -- which is what then lets the refinement (kafka_decoupled) break.
SPECIFICATION Spec
CONSTANTS MaxOffset = 3  Coupled = FALSE  Seeded = TRUE
INVARIANT INV_CaptureCoupled
