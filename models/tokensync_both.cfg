\* spec: TokenSync
\* expect: HOLDS
\* flags: -deadlock
\* 2x2 cell [both]. Capture + refresh together, silent bump reachable: the refresh closes every lag and
\* the capture is harmless belt-and-suspenders -- so it holds, same as refresh-only (capture adds nothing).
SPECIFICATION Spec
CONSTANTS MaxGen = 3  Capture = TRUE  Refresh = TRUE  AllowSilent = TRUE
INVARIANT TypeOK
INVARIANT TokenNeverLeads
PROPERTY Synced
