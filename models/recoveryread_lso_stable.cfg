\* spec: RecoveryRead
\* expect: HOLDS
\* flags: -deadlock
\* Remedy (2), alternative B (NOT adopted; A's HW bound is the
\* open alternative): a stable per-partition transactional.id. B's MANDATORY
\* initTransactions aborts A's open transaction before B writes S3, so within the lineage
\* a committed record above an open transaction is unreachable (INV_LineageSerialized) --
\* the plain read_committed endOffsets bound linearizes (RefAtomic) with NO wait and NO
\* reader-side ordering assumption: the read completes at a dangling transaction without
\* waiting it out, missing nothing.
SPECIFICATION Spec
CONSTANTS StableId = TRUE  HwTarget = FALSE  Foreign = FALSE  EndOffsetsIsLSO = TRUE  Truncation = FALSE  Tripwire = FALSE  FreezeObserved = TRUE
INVARIANT TypeOK
INVARIANT INV_ReadCommittedOnly
INVARIANT INV_LineageSerialized
PROPERTY RefAtomic
PROPERTY Terminates
